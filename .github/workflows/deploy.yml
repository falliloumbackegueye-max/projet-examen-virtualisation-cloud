name: Deploy to OpenShift Sandbox (fallilou-dev)

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NAMESPACE: fallilou-dev

jobs:
  deploy:
    name: Deploy VMs
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install oc CLI
        run: |
          curl -sLo oc.tar.gz https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux-amd64-rhel9.tar.gz
          tar -xzf oc.tar.gz
          sudo mv oc /usr/local/bin/
          oc version --client

      - name: Login to OpenShift Sandbox
        run: |
          oc login \
            --token="${{ secrets.OC_TOKEN }}" \
            --server="${{ secrets.OC_SERVER }}" \
            --insecure-skip-tls-verify=true
          echo "✅ Connecté : $(oc whoami)"

      - name: Appliquer NetworkPolicies
        run: |
          oc apply -f network/networkpolicy.yaml -n ${{ env.NAMESPACE }}
          echo "✅ NetworkPolicies appliquées"

      - name: Déployer VM1 pfSense (VM)
        run: |
          oc apply -f vms/vm1-pfsense/virtualmachine.yaml -n ${{ env.NAMESPACE }}
          echo "✅ VM1 pfSense appliquée"

      - name: Déployer VM3 DB (VM)
        run: |
          oc apply -f vms/vm3-db/virtualmachine.yaml -n ${{ env.NAMESPACE }}
          oc apply -f vms/vm3-db/service.yaml -n ${{ env.NAMESPACE }}
          echo "✅ VM3 DB appliquée"

      - name: Déployer VM2 Web (conteneur)
        run: |
          oc apply -f vms/vm2-web/deployment.yaml -n ${{ env.NAMESPACE }}
          echo "✅ VM2 Web conteneur appliqué"

      - name: Résumé final
        run: |
          echo "══════════════════════════════════"
          echo "  DÉPLOIEMENT TERMINÉ"
          echo "══════════════════════════════════"
          echo ""
          echo "── VMs (pfSense + DB) ──"
          oc get vm -n ${{ env.NAMESPACE }} \
            -o custom-columns="NOM:.metadata.name,STATUT:.status.printableStatus,PRÊTE:.status.ready"
          echo ""
          echo "── Conteneur Web ──"
          oc get deployment vm2-web -n ${{ env.NAMESPACE }} \
            -o custom-columns="NOM:.metadata.name,PRET:.status.readyReplicas,DISPO:.status.availableReplicas"
          echo ""
          echo "── DataVolumes ──"
          oc get dv -n ${{ env.NAMESPACE }} \
            -o custom-columns="NOM:.metadata.name,PHASE:.status.phase"
          echo ""
          echo "── Route VM2 ──"
          oc get route vm2-web-route -n ${{ env.NAMESPACE }} \
            -o custom-columns="NOM:.metadata.name,URL:.spec.host"
